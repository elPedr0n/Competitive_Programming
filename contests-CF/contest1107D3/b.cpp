#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

void solve() {

    string s;
    cin >> s;

    cout << pow(10, s.size()) + 1 << endl;

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    std::cin >> t;

    while (t--) {
        solve();
    }
}